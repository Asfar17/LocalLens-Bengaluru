# Vercel Environment Variables
# Configure these in your Vercel project settings (Settings > Environment Variables)

# ===========================================
# REQUIRED VARIABLES
# ===========================================

# MongoDB connection string (required)
# Use MongoDB Atlas for production: https://www.mongodb.com/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/database-name
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bangalore-assistant

# ===========================================
# OPTIONAL API KEYS (Enhanced Features)
# ===========================================

# OpenAI API Key (for AI-powered chat responses)
# Get from: https://platform.openai.com/api-keys
# Format: sk-...
OPENAI_API_KEY=sk-your-openai-api-key

# Google Cloud API Key (for Speech-to-Text, Vision OCR, Translation)
# Get from: https://console.cloud.google.com/apis/credentials
# Enable: Cloud Speech-to-Text API, Cloud Vision API, Cloud Translation API
GOOGLE_CLOUD_API_KEY=your-google-cloud-api-key

# Google Maps API Key (for Places API location recommendations)
# Get from: https://console.cloud.google.com/apis/credentials
# Enable: Places API, Geocoding API
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ===========================================
# SECURITY CONFIGURATION
# ===========================================

# Allowed CORS origins (comma-separated)
# Leave empty to allow Vercel URL automatically
# Example: https://your-app.vercel.app,https://your-custom-domain.com
ALLOWED_ORIGINS=

# ===========================================
# AUTO-CONFIGURED BY VERCEL
# ===========================================

# Node environment (automatically set by Vercel)
NODE_ENV=production

# Server port (optional, Vercel handles this automatically for serverless functions)
PORT=5000

# ===========================================
# VERCEL DEPLOYMENT NOTES
# ===========================================
# 
# 1. Create a MongoDB Atlas cluster at https://www.mongodb.com/atlas
# 2. Get your connection string from Atlas (Database > Connect > Drivers)
# 3. In Vercel project settings, add environment variables:
#    - MONGODB_URI (required)
#    - OPENAI_API_KEY (optional - enables AI responses)
#    - GOOGLE_CLOUD_API_KEY (optional - enables voice/image features)
#    - GOOGLE_MAPS_API_KEY (optional - enables location recommendations)
#    - ALLOWED_ORIGINS (optional - restricts CORS)
# 4. Ensure your MongoDB Atlas cluster allows connections from anywhere (0.0.0.0/0)
#    or add Vercel's IP addresses to the allowlist
#
# For local development:
# - Copy this file to .env
# - Update values with your actual API keys
# - Run `npm run dev` to start both frontend and backend
